pipeline {
    agent any

    stages {
        stage('Hello h') {
            steps {
                sh 'mkdir test2'
                sh 'cd test2'
                sh 'touch one.txt'
                sh 'cp one /tmp'
                echo 'welcome India'
            }
        }
    }
}
