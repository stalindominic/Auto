pipeline {
    agent any

    stages {
        stage('Hello h') {
            steps {
                sh 'mkdir test3'
                sh 'cd test3'
                sh 'touch one.txt'
                sh 'cp one.txt /tmp'
                echo 'welcome India'
            }
        }
    }
}
